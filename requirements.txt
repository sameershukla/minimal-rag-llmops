python-docx>=1.1.0          # load .docx (headings, paragraphs, tables)
pyyaml>=6.0                 # versioned configs
sentence-transformers>=3.0  # embeddings + cross-encoder reranker
chromadb>=0.5.0             # local vector DB
anthropic>=1.0              # Claude for answer generation
python-dotenv>=1.0          # load ANTHROPIC_API_KEY from .env
pytest>=8.0
